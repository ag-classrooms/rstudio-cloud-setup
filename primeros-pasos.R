## Primeros pasos y práctica
## Corre una línea a la vez

library(tidyverse)
library(estcomp)
# asignación y aritmética
x <- 5
3 * x + 5

# un vector
vector <- c(1, 3, 6, 6, 5, 2, 2, 2, 8)
sum(vector)
# otro vector
frutas <- c("manzana", "pera", "uva", "plátano")
frutas

# una gráfica simple
vector_ord <- sort(vector)
plot(vector_ord)

# Ejercicio 1: crea un vector de números, y súmale 5. ¿Qué resulta?
mis_numeros <- # llenar aquí
  
  # Ejercicio 2: qué pasa si usas plot de tus números?
  plot( # rellenar aquí)